(executable
 ((name           progress_report)
  (public_name    whatever)
  (ocamlopt_flags (-nodynlink -safe-string))
  (libraries      (stdint
                   nocrypto
                   nocrypto.unix
                   angstrom
                   hex
                   cmdliner
                   digestif
                   digestif.c
                  ))
 ))
