(library
 ((name        crcccitt_c)
  (public_name crcccitt_c)
  (c_names     (crcccitt))
  (libraries   (stdint nocrypto nocrypto.unix angstrom core hex ctypes ctypes.foreign cmdliner))
 ))

(executable
 ((name           rescue)
  (public_name    osbx)
  (ocamlopt_flags (-cclib -Wl,-E crcccitt.o))
  (libraries      (crcccitt_c stdint nocrypto nocrypto.unix angstrom core hex ctypes ctypes.foreign cmdliner))
 ))
