(jbuild_version 1)

(library
 ((name        hello)
  (public_name hello)
  (c_names     (dummy))
  (libraries   (ctypes ctypes.foreign))))

(executable
 ((name test)
  (flags     (-cclib -Wl,-E dummy.o))
  (libraries (ctypes ctypes.foreign hello))))
